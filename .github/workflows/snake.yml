name: Generate Snake

on:
workflow_dispatch:

jobs:
generate:
runs-on: ubuntu-latest

```
steps:
  - name: Generate snake
    uses: Platane/snk@v3
    with:
      github_user_name: hugorocha-web
      outputs: dist/github-contribution-grid-snake.svg
```
